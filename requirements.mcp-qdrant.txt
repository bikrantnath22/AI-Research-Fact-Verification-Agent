# ── MCP Qdrant Server — Slim Requirements ──
# Only what qdrant_server.py actually imports

# Embeddings (pulls in PyTorch — needed for embed_text / embed_batch)
sentence-transformers>=3.0.0

# Vector DB
qdrant-client>=1.10.0

# MCP
mcp>=1.1.0,<1.3.0

# Backend
fastapi>=0.115.0
uvicorn[standard]>=0.30.0

# Config
python-dotenv>=1.0.0
pydantic-settings>=2.4.0
